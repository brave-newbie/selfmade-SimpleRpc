# 📄 简历项目描述模版

## 项目名称：基于 Netty + Zookeeper 的高性能 RPC 框架

### 🛠 技术栈
**Java / Netty / Zookeeper / Spring / Protostuff / Design Patterns**

### 💻 项目描述
设计并实现了一款轻量级的分布式 RPC 框架，支持服务自动注册发现、高性能通信及 Spring 无缝集成。解决了分布式系统中服务调用复杂、性能低下的问题，深入理解了 RPC 底层原理及中间件设计思想。

### 🌟 核心职责与难点攻克

1.  **高性能网络通信架构**：
    *   基于 **Netty (NIO)** 构建通信层，设计并实现了包含魔数、版本号、消息类型的**自定义二进制协议**。
    *   利用 `LengthFieldBasedFrameDecoder` 完美解决了 TCP 长连接下的**粘包/拆包**问题，确保消息传输的完整性。
    *   引入 **Protostuff** 高性能序列化算法，替代 Java 原生序列化，将序列化后码流大小减少 **80%**，提升了传输效率。

2.  **服务治理与高可用**：
    *   集成 **Zookeeper** 作为注册中心，利用 Curator 客户端实现服务的**自动注册与发现**，支持服务节点动态上下线。
    *   实现了 **客户端负载均衡** 策略（随机/轮询），有效分散服务端压力，避免单点瓶颈。
    *   设计了基于 `IdleStateHandler` 的**双向心跳检测机制**，自动识别并清理僵死连接，保证连接池的健康度。

3.  **架构扩展性与易用性**：
    *   参考 Dubbo 设计了 **SPI (Service Provider Interface)** 扩展机制，实现了简易版 `ExtensionLoader`，支持通过配置文件动态切换序列化策略（支持 Protostuff/JSON），极大提升了框架的可扩展性。
    *   深度集成 **Spring** 容器，通过实现 `BeanPostProcessor` 接口，开发了 `@RpcService` 和 `@RpcReference` 注解。
    *   利用 **JDK 动态代理** 技术，实现了客户端对远程调用的透明化，用户无需感知底层网络细节即可像调用本地方法一样使用 RPC 服务。

### 📈 项目成果
*   成功实现了从 Demo 到企业级架构的演进，代码结构清晰，遵循 SOLID 原则。
*   具备了完整的 RPC 核心链路能力，可作为微服务架构的底层通信组件。
